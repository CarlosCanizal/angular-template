(function() {
  'use strict';

  angular
    .module('app.layout')
    .controller('Shell',Shell);

  Shell.$inject = ['$rootScope','$scope','$q','$http','$timeout','$state','$stateParams'];

  function Shell($rootScope, $scope, $q, $http, $timeout, $state,$stateParams){
        
  }

})();
